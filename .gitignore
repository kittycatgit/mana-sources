node_modules/
dist/
package-lock.json
.mana-temp/
# tooling and scaffold live in kittycatgit/mana-template
.claude/
.mana-blocked
.inbox.log
.browser-profile/

# Pipeline tooling lives in the private template repo, not in the public catalogue.
# These stay on disk here so the scripts run, but are never published.
.mcp.json
recon/
scripts/inbox.mjs
scripts/new-from-url.mjs
scripts/clear-site.mjs
scripts/gates.mjs
scripts/request-url.mjs
scripts/*.plist
.playwright-mcp/
scripts/watch-inbox.sh
.inbox.lock*/
.gh-pages-tmp/
scripts/preflight.mjs
.worktrees/

# The worktree runs link these in rather than copying, and a trailing slash only matches
# a real directory — a symlink of the same name would otherwise show up as untracked and
# fail the clean-tree check before a build starts.
.claude
node_modules
dist
